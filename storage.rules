rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read; // Anyone can read files.
      allow write: if request.auth != null; // Only authenticated users can write.
    }
  }
}
