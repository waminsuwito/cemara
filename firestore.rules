
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Hanya izinkan pengguna yang terautentikasi untuk membaca dan menulis data
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
