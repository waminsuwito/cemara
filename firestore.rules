
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow logged-in users to read from any collection
    match /{document=**} {
      allow read: if request.auth != null;
      allow write: if false; // Disallow direct client-side writes
    }
  }
}
